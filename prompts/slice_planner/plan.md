# Plan Feature Slices

Plan the feature slices for this project. The walking skeleton already exists.

**Project:** {{project_name}}
**Design digest:** {{design_digest}}
**Requirements (cite these by title in each slice for traceability):**
{{requirements}}

For each slice provide: a concrete `name`, a `description` of what "done" means, the
`requirement_titles` it satisfies, and a one-line `rationale`. Smallest coherent set,
ordered so each builds on the previous. Return the structured SlicePlan.
